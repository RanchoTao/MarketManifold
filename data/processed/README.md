# data/processed

该目录预留给清洗后的中间数据和特征数据。当前仓库根目录已提供小型可运行特征表 `sp500_features.csv`，前端使用 `public/sp500_features.csv`。

建议后续补充：

- `sp500_features.csv`：从原始价格计算得到的聚类特征；
- `feature_quality_report.csv`：缺失值、重复值、异常值和样本筛选报告。
